# Disassembly Information

The files in this directory can be used to disassemble the original disk drive ROMs.  To use, run the following from the root of the repository:

```bash
cd docs/original_roms
scripts/create_dis.sh 901483-04  # Substitute the desired ROM version
```
