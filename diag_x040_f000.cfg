MEMORY {
    ZP:  start = $0000, size = $0100, type = rw;
    ROM: start = $F000, size = $1000, fill = yes, file = %O;
}

SEGMENTS {
    DATA:    load = ROM, start = $F008, type = ro;
    CODE:    load = ROM, start = $F100, type = ro;
    VECTORS: load = ROM, start = $FFFA, type = ro;
}